FUNC void s(void){
	int idx,i;
	exec("log>MyValues.log");
	while(accel_flag==0);
	for(idx=0;idx<20000;idx++)
	{
      printf("%f %f\r\n",car.accel[2],car.raccel[2]);
	  for(i=0;i<20;i++);
	}
	exec("log off");
}